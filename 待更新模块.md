# 深夜模式
配置暗夜模式，让暗夜也有颜色，且颜色一样
目前我在手机浏览器的暗夜模式下阅读，发现页面的一些颜色和框框是灰的
我希望暗夜与正常浏览的视觉效果保持一致


# 保研和大学页面的修改：

## 关于前端的标签
把前端用于筛选的标签删去，但数据里的标签依然保留，也就是标签只在具体的卡片上展示，不用于页面上的筛选

在原来筛选标签的位置，放一个悬浮框，里面是类似于思维导图的东西，称为“成长星轨图”（这个标题用橙色小字放在悬浮框左上角的外面，就像主页“随机奇思妙想”的位置一样）。

## 星图的具体设想
1. 思维导图里面有用椭圆包裹的各个项目，也就是我在data-academic数据里写的项目标题，点击其中一个椭圆，也可以查看详情。

2. 关于每个项目椭圆的位置
项目之间用浅色发光线条或者箭头连接，同一类项目用同一种颜色（发光的浅色框框即可，你想一些与橙色或者深蓝色相配的颜色），
关于位置，我们可以在data-academic里面加上“位置”这一选填模块，可以写成“类别+顺序”，如三个项目有三个位置，分别是：“视觉1”，“视觉2.1”，“视觉2.2”，那么我们前端的展示就是左边一个椭圆，是设计1的项目标题，向右延伸出两个上下并列的椭圆，分别是设计2.1的标题和设计2.2的标题。这个表述你听懂了吗？如果我没写清楚，请向我确认
我目前想到的类别有：视觉、学术、理工、实习研究
这些类别只是帮助我们更好地排布椭圆位置，不会在主页上呈现

3. 鼠标放到某个椭圆上方
会在旁边浮现一段话，就是直接浮现出来，移开鼠标就消失的那种。
这段话，可以在data-academic里面加上“意义”这一选填模块，只会在星图里出现，不会在卡片和详情界面里显示

4. 星图与下方文章的“双向联动”
当用户点击星轨图里的某一模块时，页面会自动平滑滚动（Scroll）到下方对应的详细介绍卡片/或者直接弹出详情卡片。两种形式都行，你看看哪个好做就用哪个


# 关于保命页面
主要修改“运动”相关的卡片
export interface Exercise {
  id: string;
  name: string;
  target: number;
  current: number;
  unit: string;
  detail?: string;
  images?: string[];
  links?: Link[];
}

## 把“运动”的所有东西，替换成一个日历
是否可以在保命页面上设计设计一个日历？位于标题和筛选标签下方，详情卡片上方。这个日历记录我的所有运动，日历从2026年5月开始（之后可能还有6月、7月等等），我可以在每个日期下打卡，简单记录我在那天做了什么运动。
大部分运动是只有一句话的简单介绍的，这种就不需要设计“点进详情”
但有一部分运动，我可能想要写一些心得，这类有内容的运动，可以在日历里用发光字体标出，提醒阅读者，可以点进去看内容
下面卡片里关于运动这一类别的卡片就不需要了

此外，目前的所有data记录，你都可以为了向我呈现项目效果随意更改，我还没有把真正的经历更新上去